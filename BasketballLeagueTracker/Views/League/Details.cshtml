@model League
@{
 //TempData["TeamName"] =@Model.Name;
}

@*<div class="border p-3 mt-4">
    <div class="row pb-2">
        <a asp-controller="Player" asp-action="AddPlayerToTeam" asp-route-teamId="@Model.TeamId" class="btn btn-outline-secondary  form-control" style="width:25%">Dodaj zawodnika do drużyny</a>
    </div>
    <div class="mb-3 row p-1">

</div>*@
<div class="border p-3 mt-4">
    <div class="row pb-2 justify-content-end">
        <a asp-controller="Team" asp-action="AddTeamToLeague" asp-route-leagueId="@Model.LeagueId" class="btn btn-outline-secondary" style="width:25%">Dodaj drużynę do ligi</a>
    </div>
</div>

<div class="container mt-5">
    <h2 class="mb-4 text-center">Liga @Model.Name</h2>

    <div class="row">
        @foreach (var team in Model.Teams)
        {
            <div class="col-md-3 col-12 mb-4">
                <div class="card">
                    <div class="card-body text-center" style="max-height: 60%;">
                        <a asp-controller="Team" asp-action="Details" asp-route-teamId="@team.TeamId">
                        @if (team.TeamLogo != null)
                        {
                            <img src="data:image/jpeg;base64,@Convert.ToBase64String(team.TeamLogo)" alt="@team.Name" class="img-fluid mb-2" style="max-height: 100px;">
                        }
                        else
                        {
                            <img src="~/images/default-team.png" alt="Domyślne zdjęcie" class="img-fluid mb-2" style="max-height: 100px;">
                        }
                        <h5 class="card-title">@team.Name</h5>
                        <p class="card-text">@team.Description</p>
                        </a>
                    </div>
                </div>
            </div>
        }
    </div>
</div>
