@page
@model FavouritesModel
@{
    ViewData["Title"] = "Ulubione";
    ViewData["ActivePage"] = ManageNavPages.Favourites;
}
<div class="container mt-5 mb-5">
    <ul class="nav nav-tabs">
        <li class="nav-item">
            <a class="nav-link active" data-toggle="tab" href="#leagues">Ulubione ligi</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" data-toggle="tab" href="#teams">Ulubione zespoły</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" data-toggle="tab" href="#players">Ulubieni zawodnicy</a>
        </li>
    </ul>

    <div class="tab-content mt-2">
        <div class="tab-pane fade show active" id="leagues">
            @foreach (var league in Model.Fav.FavouriteLeagues)
            {
                <p>@league.Name</p>
            }
        </div>





        <div class="container mt-5 tab-pane fade" id="teams">
    <div class="row">
        @foreach(var team in Model.Fav.FavouriteTeams)
        {
            <div class="col-md-3 col-2 mb-4">
                <div class="card">
                        <div class="card-body text-center">
                    <a asp-controller="Team" asp-action="Details" asp-route-teamId="@team.TeamId">
                            @if (team.TeamLogo != null)
                            {
                                <img src="data:image/jpeg;base64,@Convert.ToBase64String(team.TeamLogo)" alt="@team.Name" class="img-fluid mb-2" style="max-height:150px;">
                            }
                            else
                            {
                                <img src="~/images/default-team.png" alt="Domyślne zdjęcie" class="img-fluid mb-2" style="max-height: 150px;">
                            }
                    <div class="card-body">
                        <h5 class="card-title">@team.Name</h5>
                        <p class="card-text">@team.Description</p> <!-- Zakładam, że możesz chcieć wyświetlić krótki opis drużyny -->
                    </div>
                    </a>
                </div>
                </div>
            </div>
        }
    </div>
</div>

       @* <div class="tab-pane fade" id="teams">

            @foreach (var team in Model.Fav.FavouriteTeams)
            {
                <div class="col-md-3 col-12 mb-4">

                    <div class="card">
                        <div class="card-body text-center" style="max-height: 60%;">
                            <a asp-controller="Team" asp-action="Details" asp-route-teamId="@team.TeamId">
                                @if (team.TeamLogo != null)
                                {
                                    <img src="data:image/jpeg;base64,@Convert.ToBase64String(team.TeamLogo)" alt="@team.Name" class="img-fluid mb-2" style="max-height: 100px;">
                                }
                                else
                                {
                                    <img src="~/images/default-team.png" alt="Domyślne zdjęcie" class="img-fluid mb-2" style="max-height: 100px;">
                                }
                                <h5 class="card-title">@team.Name</h5>
                            </a>
                        </div>
                    </div>
                </div>
            }
        </div>*@

        <div class="tab-pane fade" id="players">
  
            @foreach (var player in Model.Fav.FavouritePlayers)
            {
                <p>@player.Name</p>
            }
        </div>
    </div>
</div>

@*<h3>@ViewData["Title"]</h3>
<h1>:)</h1>
@foreach(var team in Model.Fav.FavouriteTeams)
{
    <h1>@team.Name</h1>
}
*@

<script src="https://code.jquery.com/jquery-3.2.1.slim.min.js"></script>
<script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/js/bootstrap.min.js"></script>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
